procs:
  backend:
    cmd: ["cmd", "/c", "cd backend && pnpm dev"]
  frontend:
    cmd: ["cmd", "/c", "cd frontend && pnpm dev"]
  drizzle-studio:
    cmd: ["cmd", "/c", "cd backend && pnpm db:studio"]